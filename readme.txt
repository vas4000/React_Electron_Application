Step1: Need to get inside the project folder location via command prompt or windows power shell.

Step2: In your computer before executing commands, make sure 'npm' is installed using "npm install" or "npm install --save-dev" command.

Step3: To run as a desktop application execute the cammand "npm run electron:serve".

(or)

Step4: To run as a web application execute the cammand "npm start".

SHORT OBJECTIVE: 
       This project can demonstrate the user interactive dynamic search and filtering features.

